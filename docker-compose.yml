services:
  frontend:
    build: .
    ports:
      - "4200:80"
    networks:
      - laundry-network
    restart: unless-stopped

networks:
  laundry-network:
    external: true
    name: laundry-pos-network
